import React from "react";
import RootLayout from "./_layout";

// Simple index that delegates to the layout which now renders the RootNavigator.
export default function Index() {
	return <RootLayout />;
}
